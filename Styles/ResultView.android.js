import React from 'react';
import {
  StyleSheet,
} from 'react-native';

export default Style = StyleSheet.create({
  container: {
    flex:1,
  },
  partText: {
    color: '#FFFFFF',
    textAlign: 'center',
    fontFamily: 'System',
    fontSize: 26
  },

});
